# Antenna Digital Twin FastAPI base URL (no trailing slash). Unset = http://localhost:8001 (matches ./run_engine.sh).
# Override if you use a different BACKEND_PORT in repo .env:
# VITE_API_URL=http://localhost:8002
